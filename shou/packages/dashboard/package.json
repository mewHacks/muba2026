{
  "name": "@shou/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --format=esm --outfile=public/app.js",
    "start": "npm run build && node --experimental-strip-types server.ts"
  },
  "dependencies": {
    "@mysten/sui": "^2.28.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
