#Claude
.omc/

.env

# Move build artifacts (Move.lock is still tracked)
shou/move/build/

# Node
node_modules/
**/node_modules/

# macOS
.DS_Store


# Seeded demo object ids — regenerated per deployment by seed-demo.ts
shou/demo-ids.json

# Build artifacts. Each is regenerated by that package's `npm start` /
# `npm run build`; a stale committed bundle is worse than an absent one,
# because it looks like the source next to it.
shou/packages/extension/dist/
shou/packages/dashboard/public/app.js
