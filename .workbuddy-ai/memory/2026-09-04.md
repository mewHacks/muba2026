# 2026-09-04

## SHOU zklogin-demo: help icon + process overlay
- File: `shou/packages/zklogin-demo/public/index.html`
- Added a help icon (?) button at top-right of the navbar (`.nav-help-btn`)
- Clicking it opens a full-screen overlay (`#process-overlay`) showing all 5 phases of SHOU's flow (Setup, Passive Protection, Send Money attempt, Sui Smart Contract intervention, Guardian Resolution & Refund)
- Overlay has: gradient header, close button, numbered phase sections, color-coded tags (green/amber/red) for risk tiers, scrollable body
- Dismissable via close button, clicking backdrop, or pressing Escape
- Also bumped hero headline "THE FUTURE OF SAFE PAYMENTS IS HERE" from 54px to 72px (`.hero-pixel-title`)
- Simplified overlay text: rewrote all 5 phases in plain English, short sentences, no jargon. Removed Move contract function names, Ed25519/BCS details, and verbose explanations. Kept color-coded risk tags.

## SHOU: Docker + one-click demo setup
- Created `Dockerfile`, `docker-compose.yml`, `.dockerignore`, `docker-start.sh`, `start-demo.sh`
- Docker runs all 4 services (enclave :3100, circuit-breaker :4000, zklogin-demo :3000, dashboard :4200) + landing page on :3080 with extension download
- `start-demo.sh` is the no-Docker alternative: installs deps, builds, starts all services
- Uses real Gonka API key (DeepSeek + MiniMax) when GONKA_API_KEY is set; falls back to dev heuristic only if key is missing
- To use real scoring: put key in shou/.env or pass GONKA_API_KEY=xxx before docker compose up
- Extension dist/ is already built and available in the repo
- demo-ids.json has pre-seeded testnet object IDs so no republishing needed
- Chrome extension must be loaded manually (can't run inside Docker) — landing page provides download + instructions

## SHOU: WebGL shader replaced with CSS gradient
- The marketing page had a WebGL fragment shader (`shader-bg.ts`) for the ambient glow behind the phone mockup
- Replaced `<canvas class="sim-shader-bg">` with `<div class="sim-shader-bg">` using an animated CSS linear-gradient
- Same pastel palette (#BFDDFF → #DCEBFF → #F3F8FF → #FFFFFF), 18s ease-in-out infinite animation
- Added `prefers-reduced-motion` fallback (static gradient)
- The `mountShaderBackground()` call in app.ts now silently no-ops (the `instanceof HTMLCanvasElement` guard is false for a div)
- Shader source (`shader-bg.ts`) left in place as dead code — removing it would require an esbuild rebuild which was failing due to a corrupted esbuild binary in node_modules
